// Background service worker
// Currently just a placeholder, but useful for future features like persistent storage or cross-origin fetching helpers.
chrome.runtime.onInstalled.addListener(() => {
    console.log("Repeater extension installed");
});
